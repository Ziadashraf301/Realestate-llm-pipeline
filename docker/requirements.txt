# Dagster + API Container Dependencies
# Matches pyproject.toml core dependencies exactly.
# NO PyTorch. NO sentence-transformers. NO BigQuery. NO OpenAI.

# --- Web Framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# --- Async HTTP ---
httpx>=0.27.0

# --- Web Scraping ---
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- ONNX Runtime (Zero PyTorch, Zero Transformers at runtime) ---
onnxruntime>=1.17.0
tokenizers>=0.19.0

# --- Vector Database ---
pymilvus>=2.5.0

# --- Caching & Storage ---
redis>=5.0.0


# --- Async ClickHouse client (HTTP-based, no native driver) ---
# httpx already handles ClickHouse HTTP API calls — no extra client needed

# --- LLM --- 
google-genai>=0.1.1
google-generativeai>=0.5.0
markdown>=3.6.0

# --- Auth ---
bcrypt>=4.1.0
pyjwt>=2.8.0

# --- Logging & Monitoring ---
structlog>=24.1.0
prometheus-client>=0.20.0

# --- Workflow Orchestration ---
dagster>=1.9.0
dagster-webserver>=1.9.0
dagster-postgres>=0.25.0

# --- MLflow & S3/MinIO Artifact Storage ---
mlflow==2.20.0
boto3>=1.34.0

# --- Data Science (for notebooks and evaluation only) ---
pandas>=2.2.0
numpy>=1.26.0
email-validator>=2.0.0